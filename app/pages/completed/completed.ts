import {Component} from '@angular/core';
import {NavController} from 'ionic-angular';

@Component({
  templateUrl: 'build/pages/completed/completed.html'
})
export class CompletedPage {
  constructor(private nav: NavController) {
  }
}
