import {Component} from '@angular/core';
import {NavController} from 'ionic-angular';

@Component({
  templateUrl: 'build/pages/pending/pending.html'
})
export class PendingPage {
  constructor(private nav: NavController) {
  }
}
